# Create and switch to new branch
git checkout -b test

# Add all changes
git add .

# Commit changes
git commit -m "Updated weather dashboard template and documentation"

# Push the new branch to remote
git push origin test
